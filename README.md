# Backend With Express-JS With MongoDB connection using Mongoose

## Description

A boilerplate code for initializing express-js backend code with DB setup(MONGODB using mongoose). It is the minimum code that will help your server up and running with mongoDB.

## Features
- express-js 
- cors 
- json & query params
- auto reload with nodemon
- mongoose and mongodb (fallback cases handled)

